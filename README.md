# @cloud-dog/tokens

Design tokens, theme primitives, and shared branding assets for Cloud-Dog web applications.

## Install

```bash
npm install @cloud-dog/tokens
```

## Usage

Import tokens into shared UI packages and app-level theme configuration. Hardcoded colour values are not permitted where an equivalent token exists.
